import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.Licence3.simo_tedom_prosper',
  appName: 'manageApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
